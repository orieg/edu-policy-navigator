// pages/districts/@cdsCode/types.ts

// Define the common PageProps if needed across pages
export type PageProps = {}

// Define more specific types for district and school data later
export interface DistrictDetails {
    [key: string]: any; // Placeholder
}

export interface SchoolDetails {
    [key: string]: any; // Placeholder
} 