// pages/index/+config.ts
// Vike automatically picks up the exported 'title'
export const title = 'Unofficial CA Education Policy Navigator';

// A default export is needed, even if empty, for Vike to process the file correctly.
export default {}; 